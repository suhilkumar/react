import React from 'react';

const ReduxToolkit = () => {
	return (
		<div>
			<h1>what is Redux</h1>
			<p>
				Redux is a library for managing state using the same techniques as
				useReducer
			</p>
		</div>
	);
};

export default ReduxToolkit;
